{
  "tasks": {
    "develop": "deno run -A --unstable-ffi npm:vite dev",
    "build": "deno run -A --unstable-ffi npm:vite build",
    "preview": "deno run --config=build/deno.json -A --unstable-ffi build/mod.ts"
  },
}
