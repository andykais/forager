{
  "tasks": {
    "test":       "deno test --allow-run=ffprobe,ffmpeg --unstable-ffi --check --allow-read --allow-write --allow-ffi --allow-env=HOME,DENO_DIR,XDG_CACHE_HOME --allow-net --trace-leaks test",
    "test:watch": "deno test --allow-run=ffprobe,ffmpeg --unstable-ffi --check --allow-read --allow-write --allow-ffi --allow-env=HOME,DENO_DIR,XDG_CACHE_HOME --allow-net --trace-leaks --watch"
  },

  "imports": {
    "@std/path": "jsr:@std/path@0.225.2",
    "@std/fs": "jsr:@std/fs@0.229.3",
    "zod": "npm:zod@3.23.8",

    "torm": "jsr:@andykais/torm@1.3.2/drivers/sqlite",
    // uncomment for testing
    "torm": "/home/andrew/Code/development/torm/drivers/sqlite.ts",
    "~/": "./src/",
    // used by the test suite
    "forager": "./src/mod.ts",
  }
}
