{
  "tasks": {
    "test":       "deno test --unstable-ffi --check --allow-read --allow-write --allow-ffi --allow-env=HOME,DENO_DIR,XDG_CACHE_HOME --allow-net --import-map=test/import_map.json test",
    "test:watch": "deno test --unstable-ffi --check --allow-read --allow-write --allow-ffi --allow-env=HOME,DENO_DIR,XDG_CACHE_HOME --allow-net  --watch"
  },

  "imports": {
    "forager": "./src/mod.ts",

    "torm": "jsr:@andykais/torm@1.3.2/drivers/sqlite",
    // uncomment for testing
    "torm": "/home/andrew/Code/development/torm/drivers/sqlite.ts",
    "zod": "npm:zod@3.23.8",
    "~/": "./src/",
  }
}
