{
  "name": "@forager/core",
  "version": "0.7.29",

  "tasks": {
    "lint": "deno lint",
    "test":       "deno test -P=test --check --trace-leaks test",
    "test:watch": "deno test -P=test --check --trace-leaks --fail-fast --watch",
    "docs:preview": "deno doc --html ./src/mod.ts && deno run --allow-read=. --allow-net=0.0.0.0:8000 jsr:@std/http/file-server docs"
  },

  "imports": {
    "~/": "./src/"
  },

  "exports": {
    ".": "./src/mod.ts",
    "./logger": "./src/lib/logger.ts"
  },

  "permissions": {
    "test": {
      "run": ["ffprobe", "ffmpeg"],
      "read": true,
      "write": true
    }
  },

  "lint": {
    "include": [
      "./src/",
      "./test/"
    ],
    "rules": {
      "tags": [],
      "include": [
        "no-unused-vars"
      ]
    }
  }
}
